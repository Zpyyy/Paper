围绕着新兴业务的 QoS 机制的问题一直是重点问题，因此如何在现有网络环境中保证用户的 QoS 质量是当务之急，是否能够根据业务分类和区分业务调度，给不同用户提供不同的业务等级，保证吞吐量等指标，
特别是当很多业务并发时，网络比较忙的时候，保证不同指标能够达到。特别是 VoIP、视频会议、IPTV 等新业务的兴起，给移动承载网带来了新的要求，这类业务要求优先级较高，且要求丢包率特别低，
这给 Qo S 机制也带来了新的挑战，现有的队列管理机制不能满足它们的极小低丢包率的要求，所以在区分式服务上要更倾向于这类业务的服务保障。
所以对于队列管理的另一个重点技术——区分式服务算法做些改进很有必要和意义。


研究内容
1、针对WRR算法只对队列的优先级进行了考虑，由分组长度致使的分组之间的优先级不公平的情况，由此提出以分组长度与队列优先级为基础的 LIWRR 调度算法。
2、为了验证队列管理机制改进算法相较于现有队列管理机制的可行性及优越性，采用 NS2 仿真软件对其进行仿真分析，将改进的算法与现有的 WRED算法进行比较，从业务的端到端丢包率、链路吞吐量、抖动等方面对改进算法的可行性和实用性进行验证。
3、针对WLAN流量回传场景，WIFI AP芯片与ONT芯片数据传输拷贝报文占用片内缓存的问题，提出零拷贝的解决方案，只将报文的前120Byte发送给入队管理模块进行处理，其余部分由出队管理模块在DDR直接读出进行处理，大大降低缓存占用，减小拥塞发生的概率，提升报文转发性能。
4、搭建硬件测试环境，用于零拷贝功能验证，针对队列拥塞、丢弃、多端口组合等异常情况下测试队列管理和调度是否正确，并从吞吐量和时延两方面验证该方案对于芯片转发性能提升的有效性。

（OLT通过ODN与ONU相连，ONU通过GE方式与AP相连，实现WLAN流量回传，成为Wi-FI建设的未来趋势）
（WOE完成WIFI AP与转发通过的数据传输,支持WIFI AP芯片与LSW数据传输,发送方向支持发送报文存储片外DDR 零拷贝报文只读报文前120字节发送给LSW，其它报文读整包并发送给LSW，完成处理；零拷贝报文在LSW处理完成后将bid_index发送给WOE进行缓存的回收。对于零拷贝报文，需要等到LSW处理完报文后，由FAM模块给出bid_index到WOE模块，WOE模块将bid_index写到寄存器中，由软件读取寄存器进行2K/4K缓存的回收。）


拟解决的关键问题
1、目前有关队列调度算法很多，包括各种PQ\RR\WRR\WFQ及其改进，但其在算法复杂度、调度公平性、以及可扩展性等方面难以达到最优，因此对于队列调度算法的优化是一个待解决的关键问题
2、
3、队列缓存是关键，零拷贝方案中头报文和身体报文的匹配，以及片内片外缓存空间如何选择是该研究中的关键。
